# ADR 0004: DD and neural-GPD inference representations

Status: accepted, 2026-08-31.

The DD baseline and an NNGPD-inspired latent function representation consume
the same stored physical realization through separate model views. The latter
is not an exact reproduction of NNGPD or fully model-independent; on DD truth
it is a DD-induced closure/representation test.
